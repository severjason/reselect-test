import styled from 'styled-components';

const BasicListStyle = styled.div`
  .selectable-row {
    cursor: pointer;
  }
  .ui.header.list-title {
    margin: 10px;
  }
`;

export default BasicListStyle;
